public class Main
{
	public static void main(String[] args) {
		
		//test the factorial method
		int number = 5;
		long result = calcFactorial(number);
		
		System.out.println("Factorial of " + number + " is: " + result);
		
	}
	
	/*
	The base case is when n is 0, in which case the method returns 1. Otherwise, it recursively calls itself with n - 1 and multiplies the result by n
	*/
	public static long calcFactorial(int n){
	   
	    if (n == 0){
	        return 1;
	    }else {
	        
	        return n * calcFactorial(n - 1);
	    }
	}
	
	
}
